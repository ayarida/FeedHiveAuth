@using FeedHiveAuth.Areas.Social.Models;
@using Microsoft.AspNetCore.Identity;
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@model FeedHiveAuth.Areas.Social.Models.ShareView

@{
    var post = Model.Post;
    var media = Model.Media;
    var socialConfigs = new SocialConfigs();
    var activeNetworkTypes = Model.ActiveSocialNetworks;

    Layout = "~/Views/Shared/_Layout.cshtml";

    var allChannels = Model.Channels.Where(channel => activeNetworkTypes.Select(networkType => networkType.ToString().ToLower()).Contains(channel.Network.ToLower()));
    var channelIds = allChannels.Select(x => x.NetworkId);
    var loggedInUser = User?.Identity?.Name;
}


<div class="editor" dir="auto" style="background: #ced4daa6; max-width: 510px">
    <div class="container pt-4 mt-4">
        <div class="channel-list d-flex flex-row gap-5 mb-5 rounded " style="background: #ced4da;padding: 20px; overflow-x: auto ">
            @foreach (var channel in allChannels)
            {

                <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>


                <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>
                <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>
                <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>

                <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>     <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>     <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>     <div class="flex-column d-flex align-items-center">
                    <img src="~/Static/Images/channelLogo.jpg" width="70" height="70" class="rounded-2 mb-2" />
                    <div style=" font-size: 14px; text-transform: capitalize;color: var(--bs-emphasis-color);">
                        @channel.Network
                    </div>
                </div>
            }
        </div>




       

            <form>
                <div class="d-flex flex-column form-container">

                    @await Html.PartialAsync("/Areas/Social/Views/Shared/_PersonViewModel.cshtml", Model)

                </div>
            </form>
            @*              <input type="submit" value="Submit" class="btn btn-primary mt-4" style="max-width: 200px;" /> *@

    </div>
</div>


<div class="page-content">
</div>

<style>

    textarea {
        width: 100%;
        height: 58px;
        padding: 12px 20px;
        box-sizing: border-box;
        border: 2px solid #ccc;
        border-radius: 4px;
        background-color: #f8f8f8;
        font-size: 14px;
        max-width: 400px;
        resize: none;
    }

    .channel-list {
        -webkit-overflow-scrolling: touch;
    }

    /* ===== Scrollbar CSS ===== */
    /* Firefox */
    * {
        scrollbar-width: thin;
        scrollbar-color: #918b92 #ffffff;
    }

        /* Chrome, Edge, and Safari */
        *::-webkit-scrollbar {
            width: 5px;
            height: 15px;
        }

        *::-webkit-scrollbar-track {
            background: #ffffff;
        }

        *::-webkit-scrollbar-thumb {
            background-color: #918b92;
            border-radius: 10px;
            border: 4px solid #ffffff;
        }


    .form-container {
        row-gap: 7px;
    }
</style>


