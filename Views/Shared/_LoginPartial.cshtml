@using Microsoft.AspNetCore.Identity
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@using FeedHiveAuth.Data.Helpers


@* <ul class="navbar-nav d-flex flex-row nav-left  mr-auto d-md-flex ">
	@if (SignInManager.IsSignedIn(User))
	{
		<li class="user-name">
			<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity?.Name!</a>
		</li>

		<form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
			<button type="submit" class="nav-link btn btn-link text-dark log-out">Logout</button>
		</form>

	}
	else
	{
		<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Register">Register</a>
		<a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">Login</a>
		<a class="nav-link text-dark" asp-area="" asp-controller="Subscriptions" asp-action="Create">New Subscription</a>
	} *@
@{
    var x = SignInManager.UserManager;
}
<ul class="navbar-nav d-flex flex-row nav-left greeting w-100 p-0">
    @if (SignInManager.IsSignedIn(User))
    {
        @*
    <a class="nav-link text-dark order-1 " asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage" style="font-size: 23px">مرحبًا @User.Identity?.Name !</a> *@

        <form class="form-inline me-auto order-3" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <span style="border-radius: 50%;color: white !important;background: #728894;width: fit-content;padding: 8px 15px;"> @User.Identity?.Name!.Substring(0, 1).ToUpper()</span>
            <div class="dropdown">
                <div style="/* border-radius: 50%;color: white !important;background: #728894;width: fit-content;padding: 8px 15px; */" type="button" id="dropdownMenuButton1" data-bs-toggle="dropdown" aria-expanded="false" class="dropdown-toggle"></div>
                <ul class="dropdown-menu account" aria-labelledby="dropdownMenuButton1">
                    <li class="dropdown-item text-center">@User.Identity?.Name</li>
                    <hr style="margin-top: 2px; margin-bottom: 3px;">
                    <a href="/Identity/Account/Manage"
                       class="list-group-item list-group-item-action py-2 text-center">
                        <i class="bi bi-person-circle"></i><span class="pe-2">الحساب</span>
                    </a>
                    <button type="submit" class="nav-link btn btn-link log-out" style="height: auto">تسجيل خروج</button>
                </ul>
            </div>
        </form>


        @*     <a href="/Home/Index" class="">
    <img src="~/uploads/logo.png" style="width: 57px;max-width: 100%;border-radius: 5px;" />
    </a> *@

        //var userIdentities = User.Identities.Select(x => x.RoleClaimType).FirstOrDefault();
        //var test = User.Claims.Where(x=>x.Value == User.Identities.)
        //var cI = User.Identities.FirstOrDefault().RoleClaimType;
        var userRoles = User.Claims.Where(x => x.Type == User.Identities.Select(x => x.RoleClaimType).FirstOrDefault()).Select(c => c.Value);
        @*         foreach (var role in userRoles)
    {
    if (role.ToLower().Equals("admin"))
    {
    <a class="nav-link text-dark" asp-area="" asp-controller="Subscriptions" asp-action="Create">New Subscription</a>

    }
    } *@
        @*    <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
    <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
    </form> *@

    }
    else
    {
        <div class="register"><a class="nav-link text-dark register" asp-area="Identity" asp-page="/Account/Register">@AppResources.Label("Register")</a></div>
        <div class="login">
            <a class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Login">@AppResources.Label("Login")</a>
        </div>
    }
</ul>

<style>
    .log-out {
        background-color: #d6081c !important;
        color: white !important;
        width: fit-content !important;
    }

        .log-out:hover {
            background-color: red;
        }

    .user-name {
        background-color: var(--green-color);
        padding: 10px;
    }

        .user-name a {
            color: white !important;
        }

    @@media (max-width: 992px) {
        .greeting {
            display: none !important;
        }
    }

    .register {
        margin: 1em;
        padding: 7px 20px;
        border: 0;
        border-radius: 25px;
        text-align: center;
        white-space: nowrap;
        cursor: pointer;
        font-size: 13px;
        background: black;
    }

    .login {
        padding: 7px 20px;
        border: 0;
        border-radius: 25px;
        text-align: center;
        white-space: nowrap;
        cursor: pointer;
        font-size: 13px;
    }

    .navbar-nav .nav-link {
        font-size: 17px;
        margin: 0;
    }

        .navbar-nav .nav-link.register {
            color: white !important;
        }

</style>